# Environment variables (example)
# Copy this file to .env.local and fill in your values

# ===========================================
# SUPABASE CONFIGURATION
# Get these values from your Supabase project: Settings > API
# ===========================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key for server-side operations (webhooks)
# Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===========================================
# STRIPE CONFIGURATION
# Get these values from https://dashboard.stripe.com/apikeys
# ===========================================

# Publishable key (safe for client-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key

# Secret key (server-side only!)
STRIPE_SECRET_KEY=sk_test_your-secret-key

# Webhook Secret
# For local testing: Use Stripe CLI (stripe listen --forward-to localhost:3000/api/stripe/webhook)
# For production: Get from https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Price ID for Premium plan
# 1. Go to https://dashboard.stripe.com/products
# 2. Create a product "BestMealMate Premium"
# 3. Add a recurring price ($4.99/month)
# 4. Copy the price ID (starts with price_)
STRIPE_PREMIUM_PRICE_ID=price_your-premium-price-id

# ===========================================
# STRIPE RECEIPT SETTINGS
# Configure these in Stripe Dashboard:
# 1. Go to Settings > Emails
# 2. Enable "Successful payments" receipts
# 3. Enable "Invoices" for subscriptions
# ===========================================
